<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homepage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SCION"
        android:roundIcon="@mipmap/scionlauncher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Homepage">
        <activity android:name=".MainActivity" />
        <activity android:name=".addsubmision" />
        <activity android:name=".CourseDetailActivity" />
        <activity android:name=".RecordedData" />
        <activity android:name=".profile_fragment" />
        <activity android:name=".coursematerialactivity" />
        <activity android:name=".reviewfile" />
        <activity
            android:name=".loginpage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>